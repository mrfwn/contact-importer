import { CsvRowCommon } from "../types";

export const validateCSVCellsValues = (csvLines: CsvRowCommon[])=> {
    let errors = [];
    csvLines.forEach((line,index) => {
        
    })
    return { error: errors } 
  }
  